[package]
name = "rostr"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.13", features = ["canvas", "image", "svg", "advanced"] }
lucide-icons = { version = "0.562", features = ["iced"] }
once_cell = "1.19"
