[package]
name = "rostr"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14", features = ["canvas", "image", "svg", "advanced", "tokio"] }
lucide-icons = { version = "0.562", features = ["iced"] }
once_cell = "1.19"
chrono = "0.4"
